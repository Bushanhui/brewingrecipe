{
  "name": "brewingrecipe",
  "private": true,
  "scripts": {
    "css:build": "npx tailwindcss -i src/styles.css -o public/css/tailwind.css --minify",
    "css:watch": "npx tailwindcss -i src/styles.css -o public/css/tailwind.css --watch",
    "dev": "npm run css:watch & npx wrangler pages dev public",
    "build": "npm run css:build",
    "deploy": "npm run build && npx wrangler pages deploy public",
    "db:create": "npx wrangler d1 create brewingrecipe-db",
    "db:schema": "npx wrangler d1 execute brewingrecipe-db --file=d1/schema.sql",
    "db:seed": "npx wrangler d1 execute brewingrecipe-db --file=d1/seed.sql",
    "db:schema:local": "npx wrangler d1 execute brewingrecipe-db --local --file=d1/schema.sql",
    "db:seed:local": "npx wrangler d1 execute brewingrecipe-db --local --file=d1/seed.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "tailwindcss": "^3",
    "typescript": "^5.7.0",
    "wrangler": "^4.65.0"
  }
}
